databases:
  - name: database_cents_sense
    databaseName: database_cents_sense
    user: database_cents_sense_user
services:
  - type: web
    name: cents_sense
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cents_sense
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
